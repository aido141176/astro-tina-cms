<nav class="bg-white border-b shadow-sm">
  <div class="px-4 mx-auto max-w-7xl">
    <div class="flex items-center justify-between h-16">

      <!-- Logo -->
      <div class="text-xl font-bold">
        MySite
      </div>

      <!-- Desktop menu -->
      <div class="hidden space-x-6 md:flex">
        <a href="/" class="hover:text-blue-500">Home</a>
        <a href="/about" class="hover:text-blue-500">About</a>

        <!-- Dropdown -->
        <div class="relative group">
        <!-- Button -->
        <button style='padding-top:0px' class="flex items-center gap-1 py-2 hover:text-blue-600">
            Services
            <span>▾</span>
        </button>

        <!-- Dropdown -->
        <div class="absolute left-0 hidden w-48 mt-0 text-gray-700 bg-white rounded shadow-md top-full group-hover:block">
            <a href="/web" class="block px-4 py-2 hover:bg-gray-100">Web Design</a>
            <a href="/seo" class="block px-4 py-2 hover:bg-gray-100">SEO</a>
        </div>
        </div>

        <a href="/contact" class="hover:text-blue-500">Contact</a>
      </div>

      <!-- Mobile button -->
      <button id="menu-btn" class="text-2xl md:hidden">
        ☰
      </button>

    </div>
  </div>

    <div id="mobile-menu" class="hidden px-4 pb-4 md:hidden">
    <a href="/" class="block py-2">Home</a>
    <a href="/about" class="block py-2">About</a>

    <!-- Mobile Dropdown -->
    <div>
        <button id="mobile-services-btn" class="w-full py-2 text-left">
        Services ▾
        </button>

        <div id="mobile-services-menu" class="hidden pl-4">
        <a href="/web" class="block py-2">Web Design</a>
        <a href="/seo" class="block py-2">SEO</a>
        </div>
    </div>

    <a href="/contact" class="block py-2">Contact</a>
    </div>
</nav>

<script is:inline>
  window.addEventListener("DOMContentLoaded", () => {
    // Mobile main menu
    const menuBtn = document.getElementById("menu-btn");
    const mobileMenu = document.getElementById("mobile-menu");

    if (menuBtn && mobileMenu) {
      menuBtn.addEventListener("click", () => {
        mobileMenu.classList.toggle("hidden");
      });
    }

    // Mobile dropdown
    const mobileServicesBtn = document.getElementById("mobile-services-btn");
    const mobileServicesMenu = document.getElementById("mobile-services-menu");

    if (mobileServicesBtn && mobileServicesMenu) {
      mobileServicesBtn.addEventListener("click", () => {
        mobileServicesMenu.classList.toggle("hidden");
      });
    }
  });
</script>