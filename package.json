{
  "name": "astro-tina-cms",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
  "dev": "tinacms dev -c \"astro dev\"",
  "build": "tinacms build && astro build",
  "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^6.0.4",
    "tinacms": "^3.6.1"
  },
  "devDependencies": {
    "@tinacms/cli": "^2.1.9"
  }
}
